<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.izhuatl">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Izhuatl"
        tools:targetApi="31">
        <activity
            android:name=".Notifications"
            android:exported="false"
            android:label="Notifications"
            android:parentActivityName=".MenuDrawer">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.izhuatl.MenuDrawer" />
        </activity>
        <activity
            android:name=".MenuDrawer"
            android:exported="false"
            android:label="@string/title_activity_menu_drawer"
            android:theme="@style/Theme.Izhuatl.NoActionBar" />
        <activity
            android:name=".ChangePassword"
            android:exported="false" />
        <activity
            android:name=".SecurityCode"
            android:exported="false" />
        <activity
            android:name=".RecoverAccount"
            android:exported="false" />
        <activity
            android:name=".Register"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>